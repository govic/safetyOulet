'use strict';

describe('Directive: productoDirective', function () {

  // load the directive's module and view
  /*beforeEach(module('pruebaAngularApp'));
  beforeEach(module('app/producto_directive/producto_directive.html'));

  var element, scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<producto-directive></producto-directive>');
    element = $compile(element)(scope);
    scope.$apply();
    //expect(element.text()).toBe('this is the productoDirective directive');
  }));*/
});