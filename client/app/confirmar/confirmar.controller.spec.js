'use strict';

describe('Controller: ConfirmarCtrl', function () {

  // load the controller's module
  /*beforeEach(module('pruebaAngularApp'));

  var ConfirmarCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    ConfirmarCtrl = $controller('ConfirmarCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });*/
});
