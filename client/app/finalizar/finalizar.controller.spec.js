'use strict';

describe('Controller: FinalizarCtrl', function () {

  // load the controller's module
  beforeEach(module('pruebaAngularApp'));
  /*
  var FinalizarCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    FinalizarCtrl = $controller('FinalizarCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });*/
});
