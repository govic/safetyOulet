'use strict';

describe('Controller: CotizarCtrl', function () {

  // load the controller's module
  /*beforeEach(module('pruebaAngularApp'));

  var CotizarCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    CotizarCtrl = $controller('CotizarCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });*/
});
