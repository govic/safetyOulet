'use strict';

describe('Controller: CambiarContrasenaCtrl', function () {

  // load the controller's module
  beforeEach(module('pruebaAngularApp'));

  /*var CambiarContrasenaCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    CambiarContrasenaCtrl = $controller('CambiarContrasenaCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });*/
});
