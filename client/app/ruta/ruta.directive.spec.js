'use strict';

describe('Directive: ruta', function () {

  // load the directive's module and view
  /*beforeEach(module('pruebaAngularApp'));
  beforeEach(module('app/ruta/ruta.html'));

  var element, scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<ruta></ruta>');
    element = $compile(element)(scope);
    scope.$apply();
    //expect(element.text()).toBe('this is the ruta directive');
  }));*/
});