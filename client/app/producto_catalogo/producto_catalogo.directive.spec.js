'use strict';

describe('Directive: productoCatalogo', function () {

  // load the directive's module and view
  /*beforeEach(module('pruebaAngularApp'));
  beforeEach(module('app/producto_catalogo/producto_catalogo.html'));

  var element, scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<producto-catalogo></producto-catalogo>');
    element = $compile(element)(scope);
    scope.$apply();
    //expect(element.text()).toBe('this is the productoCatalogo directive');
  }));*/
});