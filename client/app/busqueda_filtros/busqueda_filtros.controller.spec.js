'use strict';

describe('Controller: BusquedaFiltrosCtrl', function () {

  // load the controller's module
  beforeEach(module('pruebaAngularApp'));

  /*var BusquedaFiltrosCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    BusquedaFiltrosCtrl = $controller('BusquedaFiltrosCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });*/
});
