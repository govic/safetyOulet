'use strict';

describe('Controller: RecuperarContrasenaCtrl', function () {

  // load the controller's module
  beforeEach(module('pruebaAngularApp'));

  /*var RecuperarContrasenaCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    RecuperarContrasenaCtrl = $controller('RecuperarContrasenaCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });*/
});
