'use strict';

describe('Directive: bannerPrincipalDirective', function () {

  // load the directive's module and view
  /*beforeEach(module('pruebaAngularApp'));
  beforeEach(module('app/banner_principal_directive/banner_principal_directive.html'));

  var element, scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should make hidden element visible', inject(function ($compile) {
    element = angular.element('<banner-principal-directive></banner-principal-directive>');
    element = $compile(element)(scope);
    scope.$apply();
    //expect(element.text()).toBe('this is the bannerPrincipalDirective directive');
  }));*/
});