'use strict';

var should = require('should');
var app = require('../../app');
var request = require('supertest');

describe('GET /api/servicios', function() {

  /*it('deberia responder 200', function(done) {
    request(app)
      .post('/api/servicios')
      .expect(200);      
  });*/
});